{
  "name": "continue-visualstudio-node",
  "version": "0.0.1",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --outdir=out"
  },
  "dependencies": {
    "esbuild": "^0.23.0"
  }
}
